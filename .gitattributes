# Everything is stored and checked out with LF, whatever the contributor's platform
# or their core.autocrlf setting. A CRLF appearing in a diff is a tooling regression.
* text=auto eol=lf

# Binary assets: never touch their bytes.
*.png binary
